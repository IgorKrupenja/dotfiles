{
  "Foreach loop": {
    "prefix": "foreach",
    "description": "Foreach loop",
    "body": [
      "foreach (\\$${variable}s as \\$${variable}) {",
      "\t$0",
      "}"
    ]
  },
  "Foreach loop with key": {
    "prefix": "foreachk",
    "description": "Foreach loop",
    "body": [
      "foreach (\\$${variable}s as \\$as \\$key => \\$value) {",
      "\t$0",
      "}"
    ]
  },
  "For loop": {
    "prefix": "for",
    "description": "For loop",
    "body": [
      "for (\\$${i}=0; \\$${i} < ; \\$${i}++) {",
      "\t$0",
      "}"
    ]
  }
}